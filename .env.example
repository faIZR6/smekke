# Pre-launch gate (see proxy.ts). Unset or anything other than "true" keeps
# only /coming-soon reachable; every other page redirects there and the API
# routes 404. Set to "true" in Vercel project settings and redeploy to open
# up the full shop — no code changes required.
SITE_IS_LIVE=false
